node_modules
dist
.env
.env.local
src-tauri/target
src-tauri/gen/schemas
*.local
.DS_Store

# Large archives / build bundles — never commit (GitHub 100MB limit)
*.zip
*.7z
*.rar
*.dmg
*.exe
*.msi
mickey-website.zip
# Nested sub-projects have their own repos / deploys
# (anchored to repo root so src/components/admin etc. are NOT excluded)
/mickey-website/
/admin/
